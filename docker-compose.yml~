version: "3.9"

services:
  web:
    build: .
    command: python /code/manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/code
    ports:
      - 8000:8000
    depends_on:
      - db
    environment:
      - "DJANGO_SECRET_KEY=django-insecure-@5(49*61g-kq(-dk982t9&l8ew!rl-m%o^ud8x5mr9)(3%%zty"  
      - "DJANGO_DEBUG=True"
      - "GMAIL_USER=mgclimb@gmail.com"
      - "GMAIL_PASSWORD=dinamite1"
  db:
    image: postgres:13
    volumes:
      - postgres_data:/var/lib/postgresql/data/
    environment:
      - "POSTGRES_HOST_AUTH_METHOD=trust"

volumes:
  postgres_data: